# Modules

# the import & from statements

# to load a module
# import module_name

import math

print(math.sqrt(math.pi))

# to avoid writing math

from math import sqrt, pi

print(sqrt(pi))

# e.g use alias instead of math

import math as m

print(m.sqrt(m.pi))